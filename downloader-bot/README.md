# 🎬 ربات دانلودر همه‌کاره (Telegram)

ربات تلگرامی که از **یوتیوب، اینستاگرام، تیک‌تاک، توییتر/ایکس، فیسبوک، ردیت،
تلگرام و (اختیاری) روبیکا** — به‌علاوه‌ی صدها سایت دیگر — ویدیو و صدا دانلود می‌کند.

لینک را بفرست؛ منویی با دکمه‌ها می‌آید تا کیفیت و نوع فایل را انتخاب کنی. 🚀

---

## ✨ امکانات

- ▶️ **یوتیوب** — ویدیو با انتخاب کیفیت + استخراج صدا (MP3)
- 📸 **اینستاگرام** — پست، ریلز، IGTV
- 🎵 **تیک‌تاک** — ویدیو بدون واترمارک
- 🐦 **توییتر / ایکس** — ویدیو و GIF
- 📘 **فیسبوک** و **ردیت** و پست‌های عمومی **تلگرام**
- 🟡 **روبیکا** — (اختیاری، نیازمند اکانت)
- 🌐 هر سایت دیگری که `yt-dlp` پشتیبانی کند

### 🎛️ قابلیت‌های ویژه
| قابلیت | توضیح |
|---|---|
| 🎬 ویدیو HD | بالاترین کیفیت موجود |
| 📹 ۷۲۰p / ۴۸۰p | کیفیت کمتر → حجم کمتر |
| 🎵 صدا MP3 | **جدا کردن موزیک/صدا از ویدیو** |
| ℹ️ اطلاعات | عنوان، مدت، بازدید، لایک، تاریخ |
| 📃 لیست پخش | دانلود ۵ ویدیوی اول (یوتیوب) |
| ⏱ نمایش پیشرفت | درصد و سرعت دانلود زنده |
| 👥 لیست سفید | محدودسازی به آیدی‌های خاص |

---

## 🕹️ دستورها

| دستور | توضیح |
|---|---|
| `/start` | شروع و راهنما |
| `/help` | راهنمای کامل |
| `/mp3 <لینک>` | استخراج فقط صدا |
| `/video <لینک>` | ویدیو HD |
| `/info <لینک>` | اطلاعات ویدیو |
| `/playlist <لینک>` | دانلود لیست پخش |
| `/yt` `/insta` `/tiktok` | میان‌بر پلتفرم‌ها |
| `/rubika <لینک>` | روبیکا |
| `/stats` | آمار (فقط ادمین) |

کافی است لینک را **مستقیم** بفرستی — منوی انتخاب خودکار می‌آید.

---

## 🛠️ نصب و اجرا

### پیش‌نیازها
- Python 3.9+
- [FFmpeg](https://ffmpeg.org) (برای جدا کردن صدا و ادغام ویدیو/صدا)

### مراحل
```bash
cd downloader-bot

# ۱) محیط مجازی
python3 -m venv venv
source venv/bin/activate        # ویندوز: venv\Scripts\activate

# ۲) نصب وابستگی‌ها
pip install -r requirements.txt

# ۳) تنظیم توکن
cp .env.example .env
# سپس BOT_TOKEN را در .env قرار بده

# ۴) اجرا
python bot.py
```

### نصب FFmpeg
- **Ubuntu/Debian:** `sudo apt install ffmpeg`
- **macOS:** `brew install ffmpeg`
- **Windows:** از [ffmpeg.org](https://ffmpeg.org/download.html) دانلود کن

---

## 🔐 امنیت و حریم خصوصی

- **توکن را عمومی نکن.** اگر توکن جایی لو رفت، فوراً در **@BotFather → /revoke**
  توکن را باطل و توکن جدید بگیر، بعد `.env` را آپدیت کن.
- فایل `.env` در `.gitignore` است و داخل Git نمی‌رود.
- ربات **هیچ لاگ یا ذخیره‌ای از آیدی/فایل کاربران** نگه نمی‌دارد (فایل‌ها بعد از
  ارسال حذف می‌شوند). آمار فقط در حافظه است و با ری‌استارت پاک می‌شود.
- برای اینکه فقط خودت (یا چند نفر خاص) بتوانند از ربات استفاده کنند، در `.env`:

```ini
ALLOWED_IDS=123456789,987654321
```

---

## 🟡 تنظیم روبیکا (اختیاری)

روبیکا لینک عمومیِ مستقیم فایل نمی‌دهد؛ برای دانلود به یک اکانت روبیکا نیاز است:

```bash
pip install -U rubpy
```

سپس در `.env` یکی از این دو حالت:

```ini
# حالت ۱: با auth key
RUBIKA_AUTH=your_auth_key

# حالت ۲: با شماره موبایل و رمز
RUBIKA_PHONE=09123456789
RUBIKA_PASSWORD=your_password
```

> ⚠️ بخش روبیکا به دلیل تغییرات مکرر API و کتابخانه، «آزمایشی» است. بقیه‌ی
> پلتفرم‌ها مستقل کار می‌کنند.

---

## ⚙️ متغیرهای محیطی

| متغیر | پیش‌فرض | توضیح |
|---|---|---|
| `BOT_TOKEN` | — | توکن ربات از @BotFather |
| `ADMIN_IDS` | — | آیدی ادمین‌ها (کاما جدا) |
| `ALLOWED_IDS` | خالی (همه) | لیست سفید کاربران مجاز |
| `MAX_FILE_SIZE_MB` | `49` | حداکثر حجم ارسال فایل |
| `RUBIKA_AUTH` | — | auth key روبیکا |
| `RUBIKA_PHONE` | — | شماره اکانت روبیکا |
| `RUBIKA_PASSWORD` | — | رمز اکانت روبیکا |

---

## 🐳 اجرا با Docker

```bash
docker build -t downloader-bot .
docker run --env-file .env downloader-bot
```

## ☁️ استقرار

- **Railway / Render / Fly.io / Heroku:** فایل `Dockerfile` را کنار پروژه بگذار و
  متغیر `BOT_TOKEN` را در پنل تنظیم کن. دستور شروع: `python bot.py`.

---

## 📝 نکته‌ها

- حد مجاز آپلود ربات تلگرام **۵۰ مگابایت** است (پیش‌فرض ۴۹ MB).
- برای ویدیوهای بزرگ از `🎵 صدا MP3` یا کیفیت پایین‌تر استفاده کن.
- برخی سایت‌ها ممکن است به دلیل محدودیت جغرافیایی یا ورود اجباری دانلود نشوند.
