# ✅ چک‌لیست راه‌اندازی ربات کانال پرامپت عکس

**توکن ربات از قبل داخل پروژه تنظیم شده** — پس فقط یک کار مانده: **ربات را در کانالت ادمین کن**. تمام!

---

## ⚠️ اول این را بخوان (امنیت توکن)

توکنی که فرستادی داخل فایل `prompt-bot/index.js` قرار گرفته (`DEFAULT_PROMPT_TOKEN`). چون این ریپو **عمومی (Public)** است، **هر کسی که ریپو را ببیند می‌تواند توکن را بردارد و از ربات تو استفاده کند** (پست‌گذاشتن در کانالت، خواندن آپدیت‌ها…).

اگر این برایت مهم است، ۳ دقیقه وقت بگذار و امنش کن:
1. برو [@BotFather](https://t.me/BotFather) → `/mybots` → ربات تو → **API Token** → **Revoke current token**
2. توکن جدید را در گیت‌هاب بگذار: `github.com/seyedabadiali770-cpu/bot` → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**
   - Name: `PROMPT_BOT_TOKEN` — Secret: توکن جدید
   - (اختیاری) Name: `PROMPT_ADMIN_ID` — Secret: آیدی عددی خودت
3. بعد در `prompt-bot/index.js` مقدار `DEFAULT_PROMPT_TOKEN` را خالی کن (`''`) — این‌طوری کد هیچ توکنی ندارد و فقط از سکرت می‌خواند.

از آن به بعد، ربات امن است و بدون سکرت هم هیچ‌کس نمی‌تواند از آن استفاده کند.

---

## 🎯 ربات تو کی روشن می‌شود؟ (مهم)

اسم ربات: **@uhhghjgffggbot** — لینک مستقیم: [t.me/uhhghjgffggbot](https://t.me/uhhghjgffggbot)

ربات پرامپت کنار بقیه‌ی ربات‌ها در ورک‌فلوی `Run VPN Bot` اجرا می‌شود و آن ورک‌فلو **هر ۶ ساعت** خودکار اجرا می‌شود. یعنی بعد از اینکه این تغییرات روی شاخه‌ی `main` باشد:

- خودش سر ساعت‌های ۱۷ دقیقه (به وقت UTC) روشن می‌شود و ~۶ ساعت کار می‌کند
- اگر بخواهی همین حالا روشن شود: `Actions` → `Run VPN Bot` → **Run workflow**

> اگر ربات را در کانال ادمین کردی و پستی نیامد، یا دکمه‌ی 📮 پنل را بزن، یا یک بار `/connect @username_canal` را در چت ربات بفرست.

---

## ۱️⃣ ربات را در کانال ادمین کن

۱. در تلگرام، کانال پرامپت خودت را باز کن → **Manage Channel** → **Administrators** → **Add Admin**
۲. یوزرنیم رباتت را جست‌وجو کن و اضافه کن
۳. اجازه‌ی **Post Messages** را روشن بگذار ✔️
۴. **Save**

همین که Save بزنی، ربات پیام «🎨 ... فعال شد!» در کانال می‌گذارد و **از همان لحظه هر ساعت یک پرامپت + عکس نمونه پست می‌کند** — بدون هیچ تنظیم دیگری.

---

## ۲️⃣ با ربات حرف بزن

برو به چت خصوصی ربات → `/start`
پنل مدیریت باز می‌شود: پست فوری، پیش‌نمایش، بازه‌ی زمانی، دسته‌بندی‌ها.

---

## 🔁 اجرای ربات

ربات پرامپت کنار ربات‌های دیگر (DogsVPN و سرگرمی) در همان ورک‌فلوی `Run VPN Bot` اجرا می‌شود؛ آن ورک‌فلو هر ۶ ساعت اجرا می‌شود و هر اجرا تا ~۶ ساعت زنده می‌ماند. زمان‌بند داخلی هم شکاف‌ها را جبران می‌کند (پست عقب‌افتاده بلافاصله بعد از روشن‌شدن می‌رود).

اگر می‌خواهی همین حالا اجرا شود: `Actions` → `Run VPN Bot` → **Run workflow**.

---

## 💡 چیزهایی که ممکن است بخواهی

| می‌خواهم… | کار |
|---|---|
| پست هر ۳۰ دقیقه | در پنل: ⏱️ ساعت/بازه → «۳۰ دقیقه» (یا `/interval 30`) |
| پست هر ۲ ساعت / هر ۱۲ ساعت / روزی یک‌بار | همان منو: «۲ ساعت» / «۱۲ ساعت» / «۱ روز» |
| عدد دقیق دلخواه (مثلاً هر ۹۰ دقیقه) | همان منو → «✍️ بازه‌ی دلخواه (دقیقه)» → عدد را بفرست |
| فقط دسته‌ی خاص (مثلاً لوگو و انیمه) | پنل: 🗂️ دسته‌بندی‌ها |
| بدون عکس (فقط پرامپت متنی) | پنل: 🖼️ عکس نمونه → خاموش |
| پرامپت‌های خودم | دکمه‌ی «➕ افزودن پرامپت خودم» (ویزارد ۳ قدمی) یا `/addprompt` |
| نمونه‌کار عکس خودم | عکس را در چت ربات بفرست → روی پست‌های کانال می‌رود |
| حذف پرامپت اضافه‌شده | دکمه‌ی «📚 پرامپت‌های من» → 🗑 |
| هشتگ‌ها | دکمه‌ی «🏷️ هشتگ» در پنل (پیش‌فرض: روشن) |
| پرامپت‌های تازه با هوش مصنوعی | سکرت `AI_API_KEY` را اضافه کن (سرویس سازگار با OpenAI) → پنل: 🤖 روشن |
| عکس با کیفیت‌تر | سکرت `OPENAI_API_KEY` را اضافه کن (عکس با `gpt-image-1` ساخته می‌شود) |
| کانال روی اکانت دیگر / وی‌پی‌اس | `prompt-bot/README.md` → بخش «استقرار» |

---

## 🔐 هشدار امنیتی مهم درباره‌ی همین ریپو

در فایل‌های این ریپو، توکن‌های دو ربات دیگر **به‌صورت متن‌آشکار داخل کد** هستند (`index.js` و `fun-bot/index.js`) و چون ریپو عمومی است، همه می‌توانند آن‌ها را ببینند و از ربات‌هایت سوءاستفاده کنند:

1. در @BotFather → `/mybots` → هر ربات → **API Token** → **Revoke current token**
2. توکن جدید را در **Secrets** بگذار (مثل `BOT_TOKEN` و `FUN_BOT_TOKEN`)
3. کد را طوری بگذار که توکن را از متغیر محیطی بخواند (کد ربات پرامپت همین‌طور نوشته شده)

ربات پرامپت از همان ابتدا امن نوشته شده: بدون سکرت، هیچ توکنی در کد نیست.
